# PostgreSQL — required for saving leads, dashboard, CRM
DATABASE_URL="postgresql://user:password@localhost:5432/obsidian_prospect_engine?schema=public"

# Google Places API (New) + Geocoding — required for lead search
# Enable in Google Cloud Console; do not scrape Google Maps HTML
GOOGLE_PLACES_API_KEY="your-google-api-key"

# Optional — future AI outreach drafts
OPENAI_API_KEY=""

# Optional — future email/SMS integrations
RESEND_API_KEY=""
TWILIO_ACCOUNT_SID=""
TWILIO_AUTH_TOKEN=""
