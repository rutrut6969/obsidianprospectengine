# Obsidian Prospect Engine

Production lead generation, CRM, outreach, client management, payment tracking, and commission platform for **Obsidian Systems LLC**.

The app helps Obsidian Systems find local businesses with weak online presence, save and qualify leads, generate outreach drafts, manage campaigns, onboard clients, track Square invoices/subscriptions, and calculate lead-generator commissions.

Production domain:

```text
https://prospect.obsidian-systems.tech
```

Repository:

```text
https://github.com/rutrut6969/obsidianprospectengine
```

## Project Overview

Obsidian Prospect Engine is an internal operating system for local-business website sales.

It combines:

- Google Places lead discovery
- Website audit and lead scoring
- CRM and saved lead management
- Email/SMS/Facebook outreach draft generation
- Approval-based outbound outreach
- Campaign organization
- Client and invoice tracking
- Square payment and retainer synchronization
- Commission tracking for lead generators
- Export tools for leads and CRM data

The platform is private by default and requires authenticated access.

## Features

### Lead Generation

- Google Places API search by business category, city, state, radius, and result count.
- Website audit classification:
  - no website
  - Facebook-only
  - broken website
  - outdated website
  - has website
- Lead scoring based on online presence quality.
- Duplicate protection using:
  - Google Maps place ID
  - normalized phone number
  - normalized website URL
  - normalized business name/address fingerprint
- Email discovery from public pages:
  - homepage
  - `/contact`
  - `/about`
  - `/services`
- Extracts `mailto:` links and visible email addresses.
- Filters fake/example emails.
- Stores primary email, confidence, and contact methods.

### CRM

- Saved leads with soft delete.
- Lead ownership and visibility rules.
- Search, filter, sort, and category badges.
- Lead detail pages with:
  - business info
  - website audit
  - outreach drafts
  - email discovery
  - CRM activity history
- Export support:
  - CSV
  - XLSX
  - PDF
  - DOCX

### Outreach

- Optional OpenAI-powered draft generation.
- Fallback template generation when OpenAI is not configured.
- Draft channels:
  - email
  - SMS
  - Facebook DM
- Draft statuses:
  - `DRAFT`
  - `APPROVED`
  - `REJECTED`
  - `SENT`
  - `FAILED`
- Approval queue for editing, approving, rejecting, and sending.
- Resend email framework for approved outbound email drafts.
- Twilio SMS framework with missing-env graceful fallback, cooldowns, and delivery logging.

### Campaigns

- Campaign types:
  - `EMAIL`
  - `SMS`
- Campaign ownership by user.
- SUPER_ADMIN can see all campaigns.
- Lead generators can manage their own campaigns.
- Campaign analytics fields:
  - sent
  - failed
  - opened
  - clicked
  - replied
  - bounced
- Campaign draft generation from selected campaign leads.

### Email Templates

Reusable branded template system with:

- dark/slate Obsidian Systems styling
- purple accents
- neon green highlights
- CTA button
- footer
- unsubscribe language
- contact information

Supported variables:

```text
{{businessName}}
{{businessCategory}}
{{senderName}}
{{companyName}}
{{websiteIssue}}
{{city}}
{{ctaLink}}
```

Built-in/planned template types:

- cold outreach
- follow-up
- audit delivery
- proposal follow-up
- reminder
- abandoned inquiry follow-up

### User Roles

#### SUPER_ADMIN

Can:

- view/manage all leads
- view/manage all campaigns
- view/manage all clients
- view/manage all commissions
- manage users
- edit role, commission rate, account status, notes, and permissions
- view payout method status
- override commission records

#### LEAD_GENERATOR

Can:

- access global leads generated by SUPER_ADMIN
- create/save private leads
- manage their own campaigns
- manage their own clients
- view their own commissions
- update their own profile, avatar, payout preferences, and password

Cannot:

- see another lead generator's private leads
- see another lead generator's private campaigns
- edit role, commission rate, or account status
- view raw bank details

Legacy `USER` accounts are treated as lead generators for compatibility.

### Profile and Payouts

Profile page sections:

1. Personal Information
2. Account Information
3. Payout Settings
4. Security / Change Password
5. Activity Summary

Supported payout methods:

- Cash App
- PayPal
- Venmo
- Direct deposit
- Manual cash
- Check

Direct deposit stores only masked values for display:

- routing ending in `XXXX`
- account ending in `XXXX`

If `PAYOUT_ENCRYPTION_KEY` is configured, routing/account values are encrypted server-side using AES-256-GCM. Raw bank values are never returned to the client.

### Client Management

Clients track:

- business info
- contact info
- website package
- upfront website price
- retainer amount
- retainer billing cycle
- payment status
- client status
- Square customer/subscription references
- latest invoice/payment IDs
- last payment date
- next payment date
- payment history timeline

Client statuses:

- `PROSPECT`
- `ACTIVE`
- `OVERDUE`
- `PAUSED`
- `CANCELED`
- `COMPLETED`

Retainer payment statuses:

- `CURRENT`
- `DUE_SOON`
- `OVERDUE`
- `FAILED`
- `CANCELED`
- `PAUSED`

### Square Payments and Retainers

The app is designed to use Square-hosted payment tools. It does **not** store raw card numbers, CVV, or full payment credentials.

Stored Square references:

- Square customer ID
- Square card/payment method reference if provided by Square
- Square subscription ID
- Square invoice ID
- Square payment ID
- Square invoice URL
- payment status
- payment history timeline

Webhook endpoint:

```text
https://prospect.obsidian-systems.tech/api/webhooks/square
```

Webhook processing:

- verifies Square webhook signature
- records webhook event payload
- updates matching client payment status
- updates last/next payment dates
- records payment history
- creates in-app notifications
- creates retainer commission records when payment succeeds

### Commission Tracking

Default commission:

```text
10%
```

Commission statuses:

- `PENDING`
- `READY_TO_PAY`
- `APPROVED`
- `PAID`
- `VOIDED`

When Square confirms a retainer payment:

1. client is identified by Square customer, invoice, or subscription reference
2. assigned/onboarding lead generator is identified
3. retainer commission is calculated
4. commission is created with `READY_TO_PAY`
5. notifications are created for the lead generator and SUPER_ADMIN

### AI Systems

OpenAI is optional.

If `OPENAI_API_KEY` is missing or unavailable:

- outreach falls back to local templates
- pricing recommendations fall back to local heuristic logic
- the app does not crash

AI-supported/planned workflows:

- personalized outreach generation
- website audit summaries
- pain-point detection
- pricing recommendations
- proposal generation
- follow-up suggestions

## Tech Stack

- Next.js 15 App Router
- React 19
- TypeScript
- Tailwind CSS 4
- Prisma
- PostgreSQL
- Vercel
- Google Places API
- Resend
- Twilio
- OpenAI Responses API
- Square APIs and webhooks
- XLSX, PDFKit, DOCX export tooling

## Architecture

High-level organization:

```text
src/app
  App Router pages and API routes

src/components
  UI, CRM, campaign, sales, profile, auth, layout components

src/lib
  auth, lead services, outreach, exports, Square webhook processing,
  pricing, encryption, Google Places, website audit logic

prisma/schema.prisma
  Database schema and platform data model
```

Important service layers:

- `src/lib/auth/*` - session, role guards, visibility helpers
- `src/lib/leads/*` - duplicate protection, email discovery, lead services
- `src/lib/outreach/*` - templates, Resend, Twilio
- `src/lib/export/*` - CSV/XLSX/PDF/DOCX exports
- `src/lib/square/*` - Square webhook verification and payment sync
- `src/lib/security/*` - sensitive data encryption/masking
- `src/lib/pricing/*` - optional AI pricing with fallback

## Local Development Setup

Install dependencies:

```bash
npm install
```

Generate Prisma client:

```bash
npm run db:generate
```

Push schema to database:

```bash
npm run db:push
```

Seed super admin:

```bash
npm run db:seed
```

Run development server:

```bash
npm run dev
```

Open:

```text
http://localhost:3000
```

## Deployment

Production is deployed on Vercel.

Typical deploy workflow:

```bash
npm run build
git add .
git commit -m "Describe stable milestone"
git push origin main
npx vercel --prod --yes
```

After schema changes:

```bash
npm run db:push
```

Production domain:

```text
https://prospect.obsidian-systems.tech
```

## Environment Variables

### Core

| Variable | Required | Used By | Purpose |
| --- | --- | --- | --- |
| `DATABASE_URL` | Yes | Prisma | PostgreSQL connection string |
| `SESSION_SECRET` | Yes | Auth | Signs secure JWT session cookies, minimum 32 chars |
| `APP_URL` | Yes | Auth, emails, webhooks | Public app URL for links and webhook signature URL |
| `NEXT_PUBLIC_APP_URL` | Optional | Client references | Public app URL if client-side references are needed |
| `SUPER_ADMIN_EMAIL` | Optional | Seed/auth constants | Super admin email override if implemented |
| `ADMIN_INITIAL_PASSWORD` | Optional | Seed | Initial super admin password |

### Google

| Variable | Required | Used By | Purpose |
| --- | --- | --- | --- |
| `GOOGLE_PLACES_API_KEY` | Yes for lead search | Google Places | Places Text Search and Geocoding |

### OpenAI

| Variable | Required | Used By | Purpose |
| --- | --- | --- | --- |
| `OPENAI_API_KEY` | Optional | AI outreach/pricing | Enables AI-generated outreach and pricing |
| `OPENAI_OUTREACH_MODEL` | Optional | Outreach | Model override for outreach generation |
| `OPENAI_PRICING_MODEL` | Optional | Pricing | Model override for pricing recommendations |

### Resend

| Variable | Required | Used By | Purpose |
| --- | --- | --- | --- |
| `RESEND_API_KEY` | Required for sending | Invites/outreach | Sends invite and outbound emails |
| `RESEND_FROM_EMAIL` | Required for sending | Invites | Verified sender identity |
| `RESEND_OUTREACH_FROM_EMAIL` | Optional | Outreach | Preferred sender, e.g. `Obsidian Systems <sales@obsidian-systems.tech>` |
| `OUTREACH_TEST_TO_EMAIL` | Optional/safety | Outreach | Test recipient until real lead emails are approved |

### Twilio

| Variable | Required | Used By | Purpose |
| --- | --- | --- | --- |
| `TWILIO_ACCOUNT_SID` | Optional | SMS | Twilio account SID |
| `TWILIO_AUTH_TOKEN` | Optional | SMS | Twilio auth token |
| `TWILIO_FROM_NUMBER` | Optional | SMS | Sender phone number |
| `TWILIO_PHONE_NUMBER` | Planned alias | SMS | Alternate sender variable name |

### Square

| Variable | Required | Used By | Purpose |
| --- | --- | --- | --- |
| `SQUARE_ACCESS_TOKEN` | Required for live Square API calls | Invoices/subscriptions | Square API token |
| `SQUARE_LOCATION_ID` | Required for live Square API calls | Invoices/subscriptions | Square location |
| `SQUARE_WEBHOOK_SIGNATURE_KEY` | Required for webhooks | Webhooks | Validates Square webhook signatures |
| `SQUARE_WEBHOOK_URL` | Recommended | Webhooks | Exact notification URL registered in Square |
| `SQUARE_ENVIRONMENT` | Optional | Square | `sandbox` or `production` |

### Payout Security

| Variable | Required | Used By | Purpose |
| --- | --- | --- | --- |
| `PAYOUT_ENCRYPTION_KEY` | Recommended | Profile payouts | 32-byte base64 key or 64-char hex key for AES-256-GCM encryption |

## Square Webhook Setup

Create a webhook subscription in the Square Developer Dashboard.

Webhook URL:

```text
https://prospect.obsidian-systems.tech/api/webhooks/square
```

Configure events for invoices, payments, and subscriptions. Recommended event categories:

```text
invoice.*
payment.*
subscription.*
```

Set these env vars in Vercel:

```bash
SQUARE_WEBHOOK_SIGNATURE_KEY=
SQUARE_WEBHOOK_URL=https://prospect.obsidian-systems.tech/api/webhooks/square
SQUARE_ACCESS_TOKEN=
SQUARE_LOCATION_ID=
SQUARE_ENVIRONMENT=production
```

Signature verification uses Square's HMAC-SHA256 method with the webhook signature key, exact notification URL, and raw request body. The app compares the computed signature with the `x-square-hmacsha256-signature` header.

Important:

- The notification URL in Square must exactly match `SQUARE_WEBHOOK_URL`.
- Use HTTPS in production.
- Keep the signature key server-side only.
- Do not parse/modify the raw body before signature verification.

## Security Notes

- Session cookies are HTTP-only and signed with `SESSION_SECRET`.
- Middleware blocks unauthenticated access.
- API keys are server-side only.
- OpenAI, Twilio, Resend, and Square all fail gracefully when optional env vars are missing.
- Direct deposit details are never returned raw to the client.
- Bank details are encrypted server-side when `PAYOUT_ENCRYPTION_KEY` is configured.
- Raw card numbers and CVV are never stored.
- Square webhook signatures are verified before processing.
- Lead generator data is scoped by owner/visibility rules.

## Troubleshooting

### Build succeeds locally but production errors on database fields

Run:

```bash
npm run db:push
```

Then redeploy:

```bash
npx vercel --prod --yes
```

### Login fails with session secret error

Set:

```bash
SESSION_SECRET=your-32-character-minimum-secret
```

### Lead search fails

Check:

```bash
GOOGLE_PLACES_API_KEY=
```

Ensure Places API (New) and Geocoding API are enabled.

### Square webhook returns invalid signature

Check:

- `SQUARE_WEBHOOK_SIGNATURE_KEY`
- `SQUARE_WEBHOOK_URL`
- exact URL in Square dashboard
- production vs sandbox app credentials

### Direct deposit values are not encrypted

Set:

```bash
PAYOUT_ENCRYPTION_KEY=
```

Use either:

- 32-byte base64 key
- 64-character hex key

Without the key, the app stores only masked last-four metadata.

## Future Roadmap

- Automated payout workflows
- Stripe Connect or Dwolla payout rails
- Full Square invoice creation API integration
- Square subscription creation from client records
- Advanced analytics dashboards
- Multi-team support
- More granular permissions
- AI proposal generation
- AI follow-up recommendations
- Demo-site concept generation
- Advanced competitor analysis
- Automated onboarding workflows
- Email open/click webhook ingestion
- SMS opt-out management
- Campaign scheduling workers

## License

Private. Obsidian Systems LLC internal use only.
